{{this.audio.sounds.length}} sounds available

{{#each this.audio.sounds as |sound|}}
  <button
  {{on "click" (fn this.audio.playSound sound)}} class = "instruments">
    {{sound}}
  </button>
{{/each}}
